# Historical example only. Do not execute this file.
#
# The current hv-katra RTX 5070 Ti deployment profile is:
#   proxmox/hv-katra-rtx5070ti.yaml
#
# Runtime SSH access is supplied separately with:
#   --ssh-public-key-file FILE
#
# VM 320 now uses standard Proxmox cloud-init fields rather than committed
# user-data/vendor-data/network-config snippet paths.
#
# Keep this file only as a signpost for older documentation references.
status: retired-example
replacement: proxmox/hv-katra-rtx5070ti.yaml
